name: child workflow

on:
  push:
    branches:
      - main
      
jobs:
  reusable_workflow_job:
    name: echo
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - run: |
          echo ${{ secrets.TEST }}
